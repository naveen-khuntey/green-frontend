export default function AuditorPage() {
  return (
    <div>
      <h1 className="text-2xl font-semibold">Auditor Mode</h1>
      <p className="text-muted mt-2">Decrypt proof bundles — gated by auditor access (Lit).</p>
      <div className="mt-6 rounded-2xl bg-card p-6">Auditor UI coming soon...</div>
    </div>
  );
}
